// Firebase's web configuration identifies your project; it is safe to use in browser code.
// Replace these blank values with Firebase Console → Project settings → Your apps → SDK setup.
window.CINEWISE_FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  appId: ""
};
